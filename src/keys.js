const passport = require("passport");
const { deserializeUser } = require("passport");

module.exports = {

    database: {
        host: 'localhost',
        user: 'root',
        passport: '',
        database: 'dabase_links'
    }
}